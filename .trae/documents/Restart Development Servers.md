1. **Restart Backend Server**: Run `npm run dev` in the `backend` directory (Port 5001).
2. **Restart Frontend Server**: Run `npm run dev` in the `frontend` directory (Port 3000).
3. **Verify Access**: Confirm that both URLs are accessible.

